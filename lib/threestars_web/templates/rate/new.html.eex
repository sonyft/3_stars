<h2><%= dgettext("rate", "New Rate") %></h2>

<%= render "form.html", Map.put(assigns, :action, rate_path(@conn, :create, @currencies)) %>

<span><%= link dgettext("layout", "Back"), to: rate_path(@conn, :index) %></span>
